# Darwin User Services

Configures user-level services for macOS.

## Purpose

Manages user launchd agents and services such as sketchybar (menu bar replacement) and skhd (hotkey daemon) that run in the user session on macOS.
