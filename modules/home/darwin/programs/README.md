# Darwin User Programs

Configures user-level programs specific to macOS.

## Purpose

Each file configures a program with macOS-specific settings, including window managers (aerospace), command-line tools, and git configuration tailored for macOS.
