# NixOS User Services

Configures user-level services for NixOS.

## Purpose

Manages user systemd services such as espanso (text expansion), mako (notification daemon), and wlsunset (automatic color temperature adjustment) that run in the user session on NixOS.
