# Home Manager File Management

Manages files and directories in the user home directory.

## Features

- `directories.nix` - Creates required directories
- `executables.nix` - Installs executable scripts
- `files.nix` - Copies or links configuration files

## Purpose

Declaratively manages the user home directory structure, ensuring required directories exist and configuration files are properly placed.
