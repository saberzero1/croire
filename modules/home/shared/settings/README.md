# Shared User Settings

Cross-platform user environment settings.

## Structure

- `home/` - Home directory configuration including activation scripts, files, session variables, and shell aliases
- `xdg/` - XDG Base Directory specification settings

## Purpose

Configures shared user environment settings that apply across all platforms, providing a consistent shell environment, PATH configuration, and directory structure.
