{
  programs.nvf.settings.vim.languages.yaml = {
    enable = true;
    lsp = {
      enable = true;
      server = "yaml-language-server";
    };
    treesitter = {
      enable = true;
    };
  };
}
