{
  programs.nvf.settings.vim.languages.haskell = {
    enable = true;
    dap = {
      enable = true;
    };
    lsp = {
      enable = true;
    };
    treesitter = {
      enable = true;
    };
  };
}
