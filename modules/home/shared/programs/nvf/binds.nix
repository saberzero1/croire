{
  programs.nvf.settings.vim.binds = {
    cheatsheet = {
      enable = true;
    };
    whichKey = {
      enable = true;
    };
    hardtime-nvim = {
      enable = false;
    };
  };
  programs.nvf.settings.vim.ui.borders.plugins.which-key = {
    enable = true;
    style = "rounded";
  };
}
