# NixOS User Settings

User environment settings specific to NixOS.

## Structure

- `home/` - Home directory configuration (shell aliases, session variables, dconf settings)
- `xdg/` - XDG Base Directory specification settings including desktop entries and MIME types

## Purpose

Configures user-specific environment settings for NixOS, including Wayland-specific variables, GTK themes, and Linux desktop integration.
