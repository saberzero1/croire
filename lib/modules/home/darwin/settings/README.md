# Darwin User Settings

User environment settings specific to macOS.

## Structure

- `home/` - Home directory configuration (shell aliases, session variables, file management)
- `xdg/` - XDG Base Directory specification settings

## Purpose

Configures user-specific environment settings for macOS, including shell configuration, session variables, and file organization.
