# Home Manager Darwin Configuration

User-level configuration specific to macOS systems.

## Structure

- `programs/` - macOS-specific program configurations
- `services/` - User services for macOS
- `settings/` - macOS user preferences and environment settings

## Purpose

Contains user-level configurations that only apply when running on macOS, such as macOS-specific programs, shell settings, and XDG configurations.
