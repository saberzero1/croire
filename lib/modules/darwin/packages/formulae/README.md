# Homebrew Formulae

Contains individual formula (command-line tool) configurations for macOS.

## Purpose

Each file defines configuration for a specific command-line tool or package installed via Homebrew, including any custom build options or dependencies.
